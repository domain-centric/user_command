name: user_command
description: Extenstions on common flutter widgets that use a generic user command class and easy to use styling classes.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  overflow_view: ^0.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

